rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // HTP: all reads/writes require auth for now
    // TODO: lock down per-collection once oracle attestation model is finalised
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
